{
  "name": "ef-ble-dashboard",
  "private": true,
  "type": "module",
  "description": "Front end for the EcoFlow local dashboard. No dependencies and no build step: the browser loads the ES modules in static/js directly.",
  "scripts": {
    "test": "node --test tests/js/*.test.js"
  }
}
